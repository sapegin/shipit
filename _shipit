#compdef shipit
#autoload

arguments(){
	_values -C 'Arguments'  \
		'(-v --version)'{-v,--version}'[Print program version]' \
		'(-h --help)'{-h,--help}'[Print help (this screen)]' \
}

_arguments \
  '*:: :->subcmds' && return 0

if (( CURRENT == 1 )); then
	arguments
fi